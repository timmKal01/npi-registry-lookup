{
    "name": "npi-registry-lookup",
    "version": "0.1.0",
    "type": "module",
    "description": "Look up US healthcare providers and organizations by NPI number, name, or location via the official CMS NPPES NPI Registry API.",
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "apify": "^3.2.6"
    },
    "scripts": {
        "start": "node src/main.js"
    }
}
